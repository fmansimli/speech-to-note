import { createStore } from 'vuex'
import user from './user'
import note from './note'

export default createStore({
  state: {
  
  },
  mutations: {
  
  },
  actions: {
  
  },
  modules: {
    user,
    note
  }
})
